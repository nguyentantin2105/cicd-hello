apiVersion: v1
kind: Service
metadata:
  name: {{ include "cicd-hello.fullname" . }}
  labels:
    {{- include "cicd-hello.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cicd-hello.selectorLabels" . | nindent 4 }}